{% set root = salt['pillar.get']('project_path','/srv/www') %}
server {
    server_name {{ salt['pillar.get']('project_name') }}.{{ salt['pillar.get']('domain_name') }};
{% if 'ddll' in grains['host'] %}
    listen 8280;
{% endif %}
    {% include "sites/template.conf" %}
}
